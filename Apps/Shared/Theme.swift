import SwiftUI

/// Topo teal, the default scheme. The seed is the identity's; the derived tones follow when the
/// theme picker lands.
enum Theme {
    static let teal = Color(red: 0x1E / 255, green: 0x8C / 255, blue: 0x9E / 255)
}
